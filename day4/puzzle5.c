#include <stdio.h>
int x, y, z; // global variables
int main(){
	int i, j, k; // local variables
//	for (int i = 1; ; i++)
//		printf("%d ", i);
//	for (i =1, j = 2, k = 3;i<=10, j<=10, k<=10;i++, j++, k++)
//	for (;10;);;;;;;;;;;;;;;;
//		printf("%d : You are all very very silent!\n ", i);
//	printf("%d %d %d", i,j,k);
	printf("%d %d %d", x,y,z);
	return 0;	
}
/*
Default value for local variable is garbage value
Default value for global variable is 0
*/